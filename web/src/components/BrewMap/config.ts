/**
 * Tile provider configuration.
 * Swap this constant to change map tiles (e.g. MapTiler, Google, etc.)
 */
export const TILE_URL = 'https://tile.openstreetmap.org/{z}/{x}/{y}.png'

export const DEFAULT_CENTER: [number, number] = [-122.42, 37.77] // San Francisco
export const DEFAULT_ZOOM = 12
